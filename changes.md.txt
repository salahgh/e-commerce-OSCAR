the front is developped using next.js
the backoffice is developped using react.js

the project uses graphql in backend and front end ;
backend uses SPQR library with spring boot ;
the backoffice uses appolo client ;
for the next.JS front end propse a way for fetching graphql data;

use iText for reporting ;
don't use jasper reports;

how does graphql api get documented;
do not use redis cash;

front end build tool is vite;
package manager is npm;
front end laguage is typsript;

use graphql code generation when applicable(front end , backoffice and mobile)

do not use MUI use tailwind instead ;

do not use Emotion (MUI default)
  - CSS Modules (optionnel)
  - Styled Components (alternative)

 use use mui icons use alternative ;

 use next.js app router ;
 yup and formik for formulaires front end and backoffice ;
 date.fns for dates ;
 do not use react qury since we are using graphql ;

 do'nt use testing for the moment ;
 use prettier and do not use eslint ;
 do not use ci/cd work locally ;
 do not use git hooks ;


do not use story book ;
do not use testing on front end ;

** backoffice :
use redux for state maangemnt ;
use formik with yup ;
use MUI x charts ;

for mobile application : do not use offline mode
use expo
do not use firebase notifications



