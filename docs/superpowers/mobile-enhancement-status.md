# Mobile Enhancement Program — Status & Handoff

_Last updated: 2026-06-02. This file is the portable roadmap so work can resume on any machine. Per-milestone detail lives in `docs/superpowers/specs/` and `docs/superpowers/plans/`._

## Ground rules
- **Scope: mobile-only** (`apps/mobile`). No Vendure/`oscar-plugin` backend changes — backend-dependent features are flagged and deferred.
- **`apps/mobile` is a standalone npm project** — it has its own `package-lock.json` and is **NOT** in the pnpm/Turborepo workspace. Use **`npm`** inside `apps/mobile` (never `pnpm --filter`).
- **`apps/frontend` is the reference implementation** — mirror its patterns where they exist (e.g. slug-based product routing, checkout address model = wilaya→province + real email). Net-new mobile features (address book, etc.) are allowed where the frontend has no equivalent.
- **Process per milestone:** brainstorm → spec (`specs/`) → plan (`plans/`) → implement (TDD, commit per task) → review → merge to `main`.

## Done (all merged & pushed to `main`)
- **M0 — Stabilize + guardrails:** PDP temporal-dead-zone crash fix, slug-canonical product routing (`products/[slug].tsx`), broken route-string fixes, dead notifications-bell removal, root error boundary (`src/components/AppErrorBoundary.tsx`), real one-tap reorder, **Jest + RNTL test harness**, and the repo's **first GitHub Actions CI** (`.github/workflows/mobile-ci.yml`).
- **M1a — Checkout correctness:** guest-only email + wilaya picker added to the address step; `province` now = wilaya **name** (was `''`); real guest email (killed the fabricated `guest_<ts>@` address); prefill for logged-in users; `AlreadyLoggedInError` stale-session handling; tested helpers in `src/utils/checkout.ts` (`submitCheckoutAddress`, `buildShippingAddressInput`, `buildGuestCustomerInput`); `makeShippingAddressSchema(includeEmail)`; deleted the orphaned `app/checkout/delivery.tsx`.
- **M1b — Saved address book + checkout picker:** `profile/addresses` CRUD (list↔inline form, set-default) on the codegen'd `Create/Update/DeleteCustomerAddress` mutations; `AddressForm`/`AddressCard`; pure helpers in `src/utils/address.ts` (province↔wilaya-code round-trip + prefill mappers) with `addressFormSchema`; checkout `SavedAddressPicker` prefilling the M1a form; profile-tab entry; en/fr/ar i18n.
- **M1c — Offline-first cache + optimistic cart** (branch `m1c-offline-optimistic-cart`): in-house Apollo cache persistor (`src/apollo/persistence.ts`) over AsyncStorage — whole-cache `extract()`/`restore()`, AppState-triggered, 1 MB cap — restored via a splash gate (`src/hooks/useApolloPersistence.ts`) so the storefront/cart survive cold starts; logout purges it (`purgeApolloCache`). Optimistic `add`/`adjust`/`remove`/`clear` via pure raw-cents builders in `src/utils/optimisticCart.ts` feeding Apollo `optimisticResponse` — **every `refetchQueries` dropped**; an `update` writer links the first-ever order. PDP + reorder pass a variant snapshot. `apollo3-cache-persist` was rejected (peer-deps Apollo Client 3; will not install on this app's AC4). No offline write-queue (deferred). See `specs/2026-06-02-mobile-m1c-*` + `plans/2026-06-02-mobile-m1c-*`.
- **M1d — Payment availability gating** (branch `m1d-payment-gating`): COD stays the working checkout path; **CIB & BaridiMob render as a non-selectable "Coming soon" section** in the payment step (always shown, greyed, badged) so the backend-blocked gateway is never invoked. Pure tested helper `src/utils/payment.ts` (`getPaymentAvailability` / `isSelectableMethod` / `partitionPaymentMethods` + a local coming-soon catalog — mobile can't import `@oscar/shared`); en/fr/ar strings. The orphaned gateway screens (`app/payment/*`, `PaymentWebView`, `PaymentMethodSelector`) are left **deferred/untouched** for the future real-gateway slice (known issues: wrong `myapp://`→`oscar-fashion://` scheme, mock dead-domain URLs, `securePa yment` i18n typo). See `specs/2026-06-02-mobile-m1d-*` + `plans/2026-06-02-mobile-m1d-*`.
- **M3a — Recently-viewed products** (branch `m3a-recently-viewed`): track opened products locally (`RecentlyViewedContext` over AsyncStorage, mirrors `WishlistContext`) via a tested pure `addRecent` helper (`src/utils/recentlyViewed.ts` — dedupe-move-to-front + cap 12). A **"Recently viewed" row on Home + PDP** (PDP excludes the current product), rendered through a shared `HorizontalProductRow` **extracted from `RelatedProducts`**. The PDP records one view per open. en/fr/ar strings. See `specs/2026-06-02-mobile-m3a-*` + `plans/2026-06-02-mobile-m3a-*`.
- **M3b — Haptic feedback** (branch `m3b-haptics`): tested `src/utils/haptics.ts` over `expo-haptics` (fire-and-forget — never throws, no-ops on web). The shared `Button` fires a light impact on tap (skipped when disabled/loading); the centralized `Toast` `show()` fires success/error/warning notification haptics by type. `expo-haptics` was installed but unused. See `specs/2026-06-02-mobile-m3b-*` + `plans/2026-06-02-mobile-m3b-*`.
- **M3c — Accessibility pass** (branch `m3c-accessibility`): screen-reader semantics on the most-reused shared components — `Button` (`role="button"` + `disabled`/`busy` state + `title` label), `Input` (label/placeholder), the three `BackButton.tsx` icon controls (back/close, localized), and product cards (`ProductCardFigma` + `HorizontalProductRow`) via a tested pure `productAccessibilityLabel` (`src/utils/a11y.ts`). en/fr/ar `a11y.goBack`/`close`. Per-screen icon sweep deferred. See `specs/2026-06-02-mobile-m3c-*` + `plans/2026-06-02-mobile-m3c-*`.
- **M3d-1 — Dark-mode foundation + Settings proof** (branch `m3d1-dark-mode-foundation`): a **conventional dark palette** — `darkColors` is a twin of `colors` (same key structure) with near-black `#121212` page, `#1E1E1E` cards (elevated *lighter* than the page), off-white `#ECECEC` text, a **neutral `#ECECEC` primary** accent, and brand yellow `#FFD500` kept as the secondary "pop"; vivid status hues unchanged, pale status tints darkened. Key-parity is enforced at compile time via a widened `ColorPalette` type. Pure `getPalette(resolved)` (`src/theme/palettes.ts`) backs a `useThemeColors()` hook and a memoizing `makeThemedStyles((colors) => StyleSheet.create({…}))` factory (`src/theme/useThemedStyles.ts`) — the conversion recipe leaves the StyleSheet body unchanged because the factory param is also named `colors`. `app/profile/settings.tsx` converted end-to-end as the proof; global chrome already flips via the existing `NavigationThemeBridge`. **Twin-structural** shape was chosen over semantic role tokens so the remaining sweep stays mechanical (the user opted to diverge from the frontend's `.dark` tokens toward conventional dark). See `specs/2026-06-03-mobile-m3d1-*` + `plans/2026-06-03-mobile-m3d1-*`.
- **M3d-2 — Tabs + Home dark conversion** (branch `m3d2-tabs-home-dark`): converted the **5 bottom-tab screens** (`app/(tabs)/index|explore|orders|cart|profile.tsx`), the **shared tab bar** (`_layout.tsx`), and the **4 rendered home components** (`CategoryTabs`, `SearchHeader`, `SectionHeader`, `ProductCardFigma`) to `makeThemedStyles`/`useThemeColors` — and **routed hardcoded hex literals** (`#1E1E1E`→`primary`, `#EB3E3E`→`error`, `#FFE5E5`→`errorLight`, `#B22F2F`→`errorScale[6]`, `#F9FAFB`→`gray[1]`, `#F0F0F0`/`#EFEFEF`→`gray[2]`, `#D1D5DC`→`gray[5]`, `#999`/`#999DAF`→`text.tertiary`) to palette tokens; shadow `#000` and the gold star `#F2C94C` kept fixed. `orders.tsx`'s module-level `orderStateMap` was refactored so `getOrderStateInfo(state, colors)` takes the palette (status hues are theme-stable anyway; the two text colors now flip). Gates-only (no new render tests): zero new tsc errors, lint 0, 104 tests still green, plus a "no static `colors` import remains" guard across all 10 files. Dead home components (`CategoryScroll`/`HeroBanner`/`ProductSection`) and `PromoBanner` (no theme colors) excluded. Manual dark-toggle walkthrough still pending a device. See `specs/2026-06-03-mobile-m3d2-*` + `plans/2026-06-03-mobile-m3d2-*`.
- **M3d-3 — Products feature dark conversion** (branch `m3d3-products-dark`): converted the **PDP** (`app/products/[slug].tsx`), the **listing screen** (`app/products/index.tsx`), and **13 product components** (`ImageCarousel`, `SizeGuideModal`, `HorizontalProductRow`, `VariantPicker`, `ProductListItem`, `ProductCard`, `ProductGrid`, `SearchBar`, `FilterBottomSheet`, `FilterSheet`, `SortSheet`, `SizeSheet`, `PriceSheet`) to `makeThemedStyles`/`useThemeColors`; routed the one themeable literal (`index.tsx` `#F9FAFB`→`gray[1]`), shadows/backdrops left fixed. **Added `ThemeProvider` to `renderWithProviders`** so the PDP render test (and future screen tests) keep working; `settings.test.tsx`'s `ThemeContext` mock gained a passthrough `ThemeProvider`. `HorizontalProductRow` conversion also fixes the home "Recently viewed" row left light after M3d-2. Colorless components (`RelatedProducts`/`RecentlyViewedRow`/`FilterBar`/`ZoomableImage`) excluded. Gates-only: zero new tsc errors, lint 0, 104 tests green, import guard across all 15 files. Manual walkthrough still pending a device. See `specs/2026-06-03-mobile-m3d3-*` + `plans/2026-06-03-mobile-m3d3-*`.
- **M3d-4 — Checkout flow dark conversion** (branch `m3d4-checkout-dark`): converted the **6 live checkout files** — `app/checkout/index.tsx` (incl. the `StepItem` sub-component) + `confirmation.tsx`, and `OrderSummary`/`WilayaPicker`/`SavedAddressPicker`/`ShippingAddressForm` — to `makeThemedStyles`/`useThemeColors`. Standard recipe, no new wrinkles; the 3 decorative confetti hex particles kept fixed (`colors.primary`/`success` in that array flow through). Orphaned `PaymentMethodSelector` (M1d dead code) + colorless `_layout.tsx` excluded. Gates-only: zero new tsc errors, lint 0, 104 tests green, import guard across all 6 files. Manual dark walkthrough pending a device. See `specs/2026-06-03-mobile-m3d4-*` + `plans/2026-06-03-mobile-m3d4-*`.
- **M3d-5 — Auth screens dark conversion** (branch `m3d5-auth-dark`): converted the **8 `app/(auth)/*` files** (login/register/verify-email/verify-phone/forgot-password/reset-password/register-success + `_layout`) to `makeThemedStyles`/`useThemeColors`, and **routed 6 hardcoded hex literals** to palette tokens — `#183DE5` (link) → `info` (light hue shifts blue→cyan for dark legibility), `#6A7282` → `text.secondary`, `#FFE5E5`/`#FFCCCC` → `errorLight` (verify-phone's error-banner border kept distinct via `errorScale[2]`), `#F0F0F0` → `gray[2]`, `#F9FAFB` → `gray[1]`. `_layout.tsx`'s `screenOptions` colors wrapped in a `makeThemedStyles` object (not a `StyleSheet`). Gates-only: zero new tsc errors, lint 0, 104 tests green, import + literal guards across all 8 files. Manual walkthrough pending a device. See `specs/2026-06-03-mobile-m3d5-*` + `plans/2026-06-03-mobile-m3d5-*`.
- **M3d-6 — Orders/address detail dark conversion** (branch `m3d6-orders-address-dark`): converted `app/orders/[id].tsx` (order-detail) + `app/profile/addresses.tsx` (address book) to `makeThemedStyles`/`useThemeColors`. `[id].tsx`'s module-level `getOrderStateInfo` + `orderStateMap` refactored to `getOrderStateInfo(state, colors: ColorPalette)` (mirrors M3d-2's `(tabs)/orders.tsx`; **behavior-preserving** — all 11 order states/labels/icons unchanged, colors now palette-resolved); `addresses.tsx`'s `screenOptions` colors via the hook. `app/orders/_layout.tsx` (colorless) excluded; no literals. Gates-only: zero new tsc errors, lint 0, 104 tests green, import guard. Manual walkthrough pending a device. See `specs/2026-06-03-mobile-m3d6-*` + `plans/2026-06-03-mobile-m3d6-*`.
- **M3d-7 — Search screen dark conversion** (branch `m3d7-search-dark`): converted `app/search.tsx` (the single root-level search screen) to `makeThemedStyles`/`useThemeColors` — standard recipe, no literals, no wrinkles (only nuance: the single-`../` `'../src/theme'` import path, since it's a root-level file). `popularSearches` confirmed colorless. Gates-only: zero new tsc errors, lint 0, 104 tests green, import guard. See `specs/2026-06-03-mobile-m3d7-*` + `plans/2026-06-03-mobile-m3d7-*`.
- **M3d-8 — Profile + info screens dark conversion** (branch `m3d8-profile-info-dark`): converted the **12 remaining profile/info files** — 4 profile screens (`change-password`/`edit`/`support`/`wishlist`), 4 info screens (`contact`/`index`/`size-guide`/`faq`), and 4 components (`ProfileHeader`/`SettingsItem`/`AddressCard`/`InfoScreen`) — to `makeThemedStyles`/`useThemeColors`; routed 2 literals (`edit` `#F0F0F0`→`gray[2]`, `support` `#6A7282`→`text.secondary`). `InfoScreen`'s 4 exported sub-components each got `useStyles()`; zero-inline files (`size-guide`/`AddressCard`/`InfoScreen`) use `useStyles()` only. Gates-only: zero new tsc errors, lint 0, 104 tests green, import + literal guards. See `specs/2026-06-03-mobile-m3d8-*` + `plans/2026-06-03-mobile-m3d8-*`.
- **M3d-9 — Onboarding/splash/error-boundary dark conversion** (branch `m3d9-onboarding-ui-dark`): converted `app/onboarding.tsx`, `app/splash.tsx`, `src/components/AppErrorBoundary.tsx` to `makeThemedStyles`/`useThemeColors`. The `#2C3E50` onboarding slide background kept fixed (decorative); `AppErrorBoundary` is a **function** component (hook recipe applies — no class wrapper). Review caught a splash `StatusBar backgroundColor={colors.surface}` that had been dropped — restored (now theme-adaptive). Gates-only: zero new tsc errors, lint 0, 104 tests green, import guard. See `specs/2026-06-03-mobile-m3d9-*` + `plans/2026-06-03-mobile-m3d9-*`.
- **M3d-10 — Shared `components/ui` dark conversion** (branch `m3d10-ui-components-dark`): converted all **15 shared UI primitives** (`Avatar`/`Badge`/`Button`/`Card`/`Checkbox`/`Chip`/`Divider`/`EmptyState`/`ErrorState`/`Input`/`Loading`/`Modal`/`Select`/`Switch`/`Toast`) to `makeThemedStyles`/`useThemeColors` — the highest-impact slice, since these primitives back nearly every screen. **Per-component hooks** applied throughout the multi-component files (`Badge`×2, `Card`×4, `ErrorState`×3, `Loading`×6, `Modal`×3, plus `Toast`'s internal item + `ToastProvider`); `useThemeColors` added wherever `colors.*` is referenced outside the StyleSheet — including **color-using helper functions** (`Button.getLoaderColor`/`getTextStyle`, `Input.getBorderColor`, `Toast.getBackgroundColor`/`getIcon`), not just inline props. No literals (none present); M3b haptics preserved (`Button.light`, `Toast.success/error/warning`); `Toast`'s internal item component was renamed `ToastItemComponent` (purely internal, behavior-identical, all refs updated). **Self-enforcing gate:** removing the static `colors` import makes a missed hook a tsc error and an unneeded hook a lint unused-var, so tsc (155) + lint (0) force the correct per-component decision. Gates-only: zero new tsc errors, lint 0, 104 tests green, import guard across all 15. Combined final review APPROVED. Manual walkthrough pending a device. See `specs/2026-06-03-mobile-m3d10-*` + `plans/2026-06-03-mobile-m3d10-*`.
- **M3d-11 — `components/cart` dark conversion** (branch `m3d11-cart-dark`): converted the **5 live cart components** (`CartItem`/`CartItemContent`/`CartBadge`/`SwipeableCartItem`/`MiniCartSheet`) to `makeThemedStyles`/`useThemeColors` — the **last live surface** of the sweep. Two wrinkles: **(A)** `CartItemContent`'s **exported** `cartItemStyles` StyleSheet (consumed cross-file by `CartItem`/`SwipeableCartItem`) became an exported **`useCartItemStyles`** hook, both consumers calling it inside the component (converted in one commit, `CartItemContent` first, to avoid a broken intermediate); **(B)** `CartBadge`'s **themed default props** (`color = colors.text.primary`, `badgeColor = colors.error`) moved out of the destructuring into post-`useThemeColors()` `??` fallbacks (caller value still wins). `SwipeableCartItemWrapper` (pure passthrough) correctly got no hooks; `TabCartBadge` is `useStyles()`-only. **8 confirmed-dead components excluded** (no screen imports the `components/orders`/`navigation` barrels; the `home` trio has 0 render sites — evidence-backed) and **5 backend-gated payment surfaces deferred**. No literals. Gates-only: zero new tsc errors, lint 0, 104 tests green, import + stale-`cartItemStyles` guards. Combined final review APPROVED. Manual walkthrough pending a device. See `specs/2026-06-03-mobile-m3d11-*` + `plans/2026-06-03-mobile-m3d11-*`.
- **M3e-1 — UI primitives brand-font wiring** (branch `m3e1-ui-fonts`): kicked off the **M3e brand-font program** — applying the locale brand fonts (IBM Plex Sans Arabic for `ar`, Gabarito for fr/en) across the ~37 live components that render text via `typography.styles.*` (which carry no `fontFamily`, so they fall back to the **system font** for every locale, Arabic included). Reuses the existing `useAppFont()` hook (no new API). This slice wired the 5 text-bearing `components/ui` primitives (`Button`/`Chip`/`Divider`/`EmptyState`/`ErrorState`) — each branded `<Text>` gets `{ fontFamily: fontFamily.<weight> }` matching its **effective** weight (overrides caught: `Chip`→medium, `InlineError` icon→bold), merged **before** any caller `textStyle` to preserve override-ability; emoji-only nodes left unwired. **Unlike M3d, this sweep is NOT self-enforcing** (a missed `<Text>` still compiles) — gates + a per-file `<Text>`/`fontFamily` coverage heuristic + review are the checks; the device Arabic walkthrough is the appearance gate. Gates-only: zero new tsc errors, lint 0, 104 tests green. See `specs/2026-06-03-mobile-m3e-*` + `plans/2026-06-03-mobile-m3e1-*`.

**Health:** 104 unit tests pass (21 suites); `npm run lint` = 0 errors (warnings are pre-existing). M0/M1a/M1b/M1c/M1d/M3a/M3b/M3c/M3d-1/M3d-2/M3d-3/M3d-4/M3d-5/M3d-6/M3d-7/M3d-8/M3d-9/M3d-10/M3d-11/M3e-1…8 source is `tsc`-clean (baseline still 155 pre-existing errors; **zero new** introduced).

## Next up (recommended order)
**M3 (UX polish) is being built slice-by-slice — M3a (recently-viewed), M3b (haptics), M3c (accessibility), M3d-1 (dark-mode foundation + Settings proof), M3d-2 (tabs + home dark conversion), M3d-3 (products feature dark conversion), and M3d-4 (checkout flow dark conversion) are done.** Remaining: the rest of the M3d screen-by-screen dark sweep.

### M3d — Real dark mode (live screen sweep COMPLETE)
This is a *program*, not a single slice. Blast radius (measured 2026-06-02): **62 files import the static `colors`**, across **~93 `StyleSheet.create` blocks** and **~860 `colors.*` usages**. The theme toggle exists (`app/profile/settings.tsx` → `setMode`) and `ThemeContext` resolves `light`/`dark`; the global chrome flips via `NavigationThemeBridge` in `app/_layout.tsx`.

**Sub-slices:**
- **M3d-1 — Foundation + Settings proof — ✅ DONE** (branch `m3d1-dark-mode-foundation`): conventional dark palette (`darkColors` twin of `colors`), pure `getPalette()` + `useThemeColors()` + memoizing `makeThemedStyles()`, theme-barrel re-exports, and `app/profile/settings.tsx` converted end-to-end as the proof. The conversion recipe is established. See `specs/2026-06-03-mobile-m3d1-*` + `plans/2026-06-03-mobile-m3d1-*`.
- **M3d-2 — Tabs + Home conversion — ✅ DONE** (branch `m3d2-tabs-home-dark`): the 5 tab screens + tab bar + 4 rendered home components, incl. hardcoded-literal routing. Established two extra recipe wrinkles for the rest of the sweep: (1) **inline JSX `color={…}` props** need `const colors = useThemeColors()` in the component (not just the StyleSheet factory); (2) **module-level `colors` usage** (maps/constants that run at import) must be refactored to take the palette as a parameter (see `orders.tsx` `getOrderStateInfo`) or moved into the component.
- **M3d-3 — Products feature conversion — ✅ DONE** (branch `m3d3-products-dark`): PDP + listing screens + 13 product components, plus the `renderWithProviders` ThemeProvider fix. Three more recipe wrinkles established: (1) **`renderWithProviders` wraps `ThemeProvider`** now — any test that *mocks* `ThemeContext` must add a passthrough `ThemeProvider` to its mock; (2) **always-dark surfaces** (image lightbox, "Out of Stock" overlays) use `colors.white`, not `colors.text.inverse` (which flips dark and would vanish); (3) **colorless StyleSheets** (e.g. `ProductGrid`) stay a plain `StyleSheet.create` — don't wrap them in `makeThemedStyles` (unused `colors` param); just add `useThemeColors()` for inline props.
- **M3d-4 — Checkout flow conversion — ✅ DONE** (branch `m3d4-checkout-dark`): the 2 checkout screens + 4 components, standard recipe (no new wrinkles); confetti hex literals kept; orphaned `PaymentMethodSelector` + colorless `_layout.tsx` excluded.
- **M3d-5 — Auth screens conversion — ✅ DONE** (branch `m3d5-auth-dark`): the 8 `app/(auth)/*` files, routing 6 hex literals to palette tokens (`#183DE5`→`info`, `#6A7282`→`text.secondary`, `#FFE5E5`/`#FFCCCC`→`errorLight`, `#F0F0F0`→`gray[2]`, `#F9FAFB`→`gray[1]`). New wrinkle established: **`screenOptions` colors** (used outside a StyleSheet, e.g. `_layout.tsx` header) wrap in a `makeThemedStyles` factory returning a plain object.
- **M3d-6 — Orders/address detail conversion — ✅ DONE** (branch `m3d6-orders-address-dark`): `app/orders/[id].tsx` + `app/profile/addresses.tsx`; reused the M3d-2 module-level-`colors` wrinkle (`getOrderStateInfo(state, colors)` param refactor, behavior-preserving). `app/orders/_layout.tsx` colorless-excluded.
- **M3d-7 — Search screen conversion — ✅ DONE** (branch `m3d7-search-dark`): `app/search.tsx` (single file, standard recipe, single-`../` import path).
- **M3d-8 — Profile + info screens conversion — ✅ DONE** (branch `m3d8-profile-info-dark`): 12 files (4 profile + 4 info screens + 4 components), 2 literals routed. Multi-export component files (`InfoScreen`) need `useStyles()` in EACH exported component.
- **M3d-9 — Onboarding/splash/error-boundary — ✅ DONE** (branch `m3d9-onboarding-ui-dark`): 3 files; `#2C3E50` slide bg kept fixed; `AppErrorBoundary` is a function component. New gotcha: don't *drop* an inline themed prop to dodge `useThemeColors` (a converted file's StatusBar bg was wrongly removed, then restored) — keep the prop, add the hook.
- **M3d-10 — Shared `components/ui` (15 primitives) — ✅ DONE** (branch `m3d10-ui-components-dark`): all 15 converted (`Avatar`/`Badge`/`Button`/`Card`/`Checkbox`/`Chip`/`Divider`/`EmptyState`/`ErrorState`/`Input`/`Loading`/`Modal`/`Select`/`Switch`/`Toast`). Per-component hooks in EVERY multi-component file (`Card`×4, `Loading`×6, `Badge`×2, `ErrorState`×3, `Modal`×3, `Toast`'s item + provider); `useThemeColors` added for **color-using helper functions** (`Button.getLoaderColor`, `Toast.getBackgroundColor`), not just inline props — the gate self-enforces via tsc(155)+lint(0). M3b haptics preserved; no literals. Combined final review APPROVED.
- **M3d-11 — `components/cart` (5 live) — ✅ DONE** (branch `m3d11-cart-dark`): `CartItem`/`CartItemContent`/`CartBadge`/`SwipeableCartItem`/`MiniCartSheet`. Wrinkle A: exported `cartItemStyles` → `useCartItemStyles` hook, both cross-file consumers call it (one commit, `CartItemContent` first). Wrinkle B: `CartBadge` themed default props → post-hook `??` fallbacks. Passthrough `SwipeableCartItemWrapper` got no hooks. **This completes the dark sweep for every live screen.** Combined final review APPROVED.
- **Post-M3d follow-ups:** the M3d **live** screen sweep is **done** — every rendered screen/component now themes. Remaining, in rough priority: (1) **Arabic-font wiring** → ✅ done as **M3e** (see the M3e section below); (2) **`darkColors` tint refinement** → ✅ resolved: an M3e-era audit found the provisional `*Scale` low-ends (1–3) are *unused* in dark (`secondaryScale` and `success`/`warning`/`infoScale` are referenced by no component); `secondaryLight` (#3D3517) and `errorScale[6]` read fine; the one consumed nit — `errorScale[2]`, the verify-phone error-banner border — was darkened `#FF9999`→`#7A3333` (muted brick-red on the dark `errorLight` bg) and the rest documented in-code (`darkColors.ts`); (3) **dead-code decision** for the 8 excluded components (`components/orders` ×3, `components/navigation` ×2, `components/home` ×3 — no screen imports them): theme-for-completeness or delete; (4) **deferred payment dark pass** (`app/payment/*`, `PaymentWebView`, `PaymentMethodSelector`) — convert alongside the real CIB/BaridiMob gateway work; (5) **on-device manual dark walkthrough** of the whole app (each slice's manual gate was deferred pending a device).

### M3e — Brand-font application (✅ COMPLETE — all 37 live components wired)
A *program* (sibling to M3d): a working `useAppFont()` hook exists and **18** components use it, but **43 live-ish components** render text via `typography.styles.*` with **no `fontFamily`** → the **system font** for every locale (Arabic included). M3e wires the brand fonts (IBM Plex Sans Arabic for `ar`, Gabarito for fr/en) across the **37 live** components (6 dead/deferred excluded, same set as M3d). Recipe: add `useAppFont()`, merge `{ fontFamily: fontFamily.<weight> }` per branded `<Text>`/`<TextInput>` matching its **effective** weight; StyleSheets unchanged (the named family is authoritative, residual `fontWeight` harmless). **Not self-enforcing** — completeness relies on a per-file `<Text>`/`fontFamily` coverage heuristic + review + the eventual device walkthrough. Full sweep was user-selected (fr/en also moves system→Gabarito, per Figma intent). See `specs/2026-06-03-mobile-m3e-*`.

**Sub-slices** (≈8, by feature area):
- **M3e-1 — UI primitives — ✅ DONE** (branch `m3e1-ui-fonts`): `Button`/`Chip`/`Divider`/`EmptyState`/`ErrorState` (5). Pattern validated: family merged **before** any caller `textStyle`; effective-weight overrides handled (`Chip`→medium, `InlineError` icon→bold); emoji-only nodes skipped.
- **M3e-2 — Tabs — ✅ DONE** (`a968b11`): `app/(tabs)/{_layout,orders}` (cart badge→bold; orders 18 nodes across `OrderItem`+`OrdersScreen`/5 branches).
- **M3e-3 — Products — ✅ DONE** (`409cfb3`): 4 components + `app/products/[slug]` (PDP 13 nodes; `SearchBar` `TextInput` wired; ad-hoc weights handled).
- **M3e-4 — Checkout — ✅ DONE** (`ffed850`): 4 components + `app/checkout/{index,confirmation}` (`index` 33 nodes; `StepItem` own hook; `WilayaPicker` runtime active-weight switch). `PaymentMethodSelector` excluded.
- **M3e-5 — Auth — ✅ DONE** (`96655e3`): `app/(auth)/{forgot-password,reset-password,verify-email}` (inline `fontWeight: bold` overrides → `fontFamily.bold`).
- **M3e-6 — Profile — ✅ DONE** (`4b96cb4`): 3 components + `app/profile/{change-password,settings,wishlist}` (`settings` test-covered; emoji flags skipped; `wishlist` `renderItem` helper closed over `fontFamily`).
- **M3e-7 — Info — ✅ DONE** (`61cc51f`): `app/info/{contact,faq,index,size-guide}` + `InfoScreen` (4 sub-components each got their own hook).
- **M3e-8 — Orders/cart/root — ✅ DONE** (`72d723a`): `app/orders/[id]` (25 nodes) + `components/cart/{CartItemContent,MiniCartSheet}` + `AppErrorBoundary`. `app/splash.tsx` is a no-op (0 `<Text>` nodes); `getOrderStateInfo` left alone (pure data fn, no JSX).
- **✅ M3e COMPLETE** — verified by `comm`: the only files still on `typography.styles` without `useAppFont` are the **6 intentionally-excluded** (dead `components/orders` ×3, deferred `app/payment/status` + `components/payment/PaymentWebView`, dead `PaymentMethodSelector`) + `app/splash.tsx` (renders no text). **All 37 live components wired** — Arabic now renders in IBM Plex Sans Arabic and fr/en in Gabarito across every live screen. tsc 155, lint 0, 104/21 throughout.

**Recommended next action:** both **M3d (dark mode)** and **M3e (brand fonts)** live sweeps are complete. The key remaining step is the **on-device Arabic + dark walkthrough** (`cd apps/mobile && npx expo start`, toggle `ar` + dark) — the one appearance gate the automated checks (tsc/lint/tests) can't cover, deferred throughout for lack of a device. Other follow-ups: the **dead-code decision** (theme-or-delete the 8 excluded components); the **deferred payment** dark + font pass alongside the real CIB/BaridiMob gateway work. (The `darkColors` tint refinement is ✅ done — see the Post-M3d note above.)

Then (largely backend-dependent, deferred under mobile-only):
4. **M2 — Engagement/retention** (reviews, push notifications, wishlist sync) — recently-viewed already shipped in M3a.
5. **M4 — Native capabilities & release hardening** (deep links, EAS OTA, biometric, camera/media, typed `app.config.ts` + endpoint hardening).
6. **Real CIB/BaridiMob gateway** — resumes once backend `oscar-plugin` payment work lands (M1d gated it as "coming soon").

> **Real CIB/BaridiMob online payment is backend-blocked** (gateway *initiate* + settlement metadata). M1d gated it as "coming soon"; the real WebView/deep-link gateway slice resumes once the backend `oscar-plugin` payment work lands.

## Deferred minors (small follow-ups)
- M1a: localize the reorder toasts (`orders.reorder*`) in fr/ar; stale-session recovery (logout) action; show wilaya in the checkout review step.
- M1b: editing a non-app-created address whose `province` isn't an exact wilaya `name` drops the wilaya selection; no explicit "enter a new address" chip at checkout for signed-in users.
- M1c: **offline write-queue deferred** (NetInfo + queue/replay cart mutations while offline) — reads survive offline and the UI is optimistic, but cart *writes* still need connectivity. Optimistic totals are a raw-cents estimate reconciled by the `cache-and-network` server response. Persistence's AppState auto-trigger is runtime-verified, not unit-tested (see runbook §6).

## Known tech debt (not introduced by this program)
- **`tsc` baseline is red** (~150 pre-existing errors): the generated `src/graphql/generated/graphql.ts` references `QueryResult`, removed in Apollo Client 4 (the `typescript-react-apollo` codegen vs Apollo v4 mismatch), cascading into `src/apollo/client.ts` + components; plus `i18n compatibilityJSON: 'v3'`, and `app/(tabs)/profile.tsx` reads `customer.customFields` not in the `CustomerFields` fragment. **CI type-check is intentionally non-blocking** (`continue-on-error: true`) until this is fixed (its own effort; needs the backend running for codegen). New work must add **zero new** tsc errors.
- `expo lint` = 0 errors but ~420 pre-existing warnings (mostly `@typescript-eslint/array-type`).
- When counting tsc errors by file, use `grep -oE "^\S+\.tsx?"` — the pattern `^[^(]+` silently drops paren-paths (`app/(tabs)/...`, `app/(auth)/...`).

## Resume on another machine
```bash
git clone https://github.com/salahgh/e-commerce-OSCAR.git   # or: git pull origin main
cd e-commerce-OSCAR/apps/mobile
npm install            # standalone npm project — NOT pnpm
npm test               # 104 tests should pass (21 suites)
npm run lint           # 0 errors expected
# read docs/superpowers/ (this file, specs/, plans/) for full context
```
Backend (only needed for GraphQL codegen, not to read/continue): see root `CLAUDE.md`.
